import { TelephonePipePipe } from './telephone-pipe.pipe';

describe('TelephonePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TelephonePipePipe();
    expect(pipe).toBeTruthy();
  });
});
